@model AlquilerVehiculos.Models.TVehiculosTipo

@{
    ViewData["Title"] = "Eliminar Módelo de Vehículo";
}

<h1 class="mb-4">Eliminar Módelo de Vehículo</h1>

<div class="card shadow-sm">
    <div class="card-body">

        <h4 class="card-title mb-3">
            Confirmación de eliminación
        </h4>
        <hr />

        <div class="alert alert-danger" role="alert">
            ¿Está seguro que desea eliminar este módelo?
            <strong>Esta acción no se puede deshacer.</strong>
        </div>

        <dl class="row">

            <dt class="col-sm-3 fw-bold">@Html.DisplayNameFor(model => model.Descripcion)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Descripcion)</dd>

            <dt class="col-sm-3 fw-bold">@Html.DisplayNameFor(model => model.TarifaDiaria)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.TarifaDiaria)</dd>

        </dl>

        <div class="d-flex justify-content-between mt-4">

            <a asp-action="Index" class="btn btn-secondary">
                Volver a la lista
            </a>

            <form asp-action="Delete" method="post" class="m-0">
                @* Ocultamos el Id del cliente para el POST *@
                <input type="hidden" asp-for="IdTipo" />
                <button type="submit" class="btn btn-danger">
                    Eliminar Vehiculo Tipo
                </button>
            </form>

        </div>

    </div>
</div>
