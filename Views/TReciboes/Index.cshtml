@model IEnumerable<AlquilerVehiculos.Models.TRecibo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Metodo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Monto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Metodo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRecibo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRecibo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRecibo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
